name: Rüya Tabirleri JSON Scraper

on:
  workflow_dispatch: # Sadece manuel tetikleme ile çalışır

permissions:
  contents: write

jobs:
  scrape-and-commit:
    runs-on: ubuntu-latest
    
    steps:
      - name: Repoyu Klonla
        uses: actions/checkout@v3

      - name: Python'u Kur
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - name: Bağımlılıkları Yükle
        run: |
          python -m pip install --upgrade pip
          # cloudscraper eklendi, o da kendi içinde gereken requests altyapısını kurar.
          pip install cloudscraper beautifulsoup4
          
      - name: Scraper'ı Çalıştır
        run: python scraper.py
        
      - name: Değişiklikleri Commit Et ve Pushla
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add veri/*.json
          git commit -m "JSON verileri güncellendi" || echo "Değişiklik yok, commit atlanıyor."
          git push
